import {GraphUtils} from "./GraphUtils";

export class Mouse {
    constructor(canvas, stationManager) {
        // const canvasElement = document.getElementById(canvas.id);
        // const rect = canvasElement.getBoundingClientRect();
        // canvasElement.addEventListener('mousedown', event => {
        //    const x = event.clientX;
        //    const y = event.clientY;
        //    const xy = {x: x, y: y};
        //    stationManager.stationMap.forEach(station => {
        //        const stationWasCLicked = GraphUtils.distanceBetweenPoints(station, xy) < 10;
        //        if(stationWasCLicked) {
        //            console.log('Clicked Station:');
        //            console.log(station);
        //        }
        //    });
        //
        // });
    }
}