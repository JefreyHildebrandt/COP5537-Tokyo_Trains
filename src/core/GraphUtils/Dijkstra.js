export class Dijkstra {
    static alreadyCalculated = new Map();

    static getDistanceBetweenTwoStations(stationMap, id1, id2) {
        return 0;
    }

    static getPathBetweenTwoStations(stationMap, id1, id2) {
        return [];
    }
}