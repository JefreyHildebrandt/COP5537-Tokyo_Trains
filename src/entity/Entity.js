export class Entity {
    rotation = 0;
    constructor(props) {
        this.x = props.x;
        this.y = props.y;
        this.image = props.image;
        this.id = props.id;
    }

    draw(ctx) {

    }
}